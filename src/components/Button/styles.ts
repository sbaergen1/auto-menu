import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    common: {
        padding: 8,
        borderRadius: 4,
        backgroundColor: 'white',
        alignItems: 'center'
    },
    disabled: {
        opacity: 0.3
    }
})